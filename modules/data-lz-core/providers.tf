// Provider configuration removed to allow the root module (stack) to
// pass provider configurations and aliases (for example `azurerm` and
// `azurerm.hub`).  The provider requirement stays in versions.tf.
